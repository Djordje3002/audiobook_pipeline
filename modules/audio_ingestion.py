"""Whisper transcription, chunking, overlap handling, and segment grouping."""


def transcribe_serbian_audio(source_path: str) -> list[dict]:
    """Return timestamped transcript segments from Serbian input audio."""
    return []
